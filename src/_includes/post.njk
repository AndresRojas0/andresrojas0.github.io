---
layout: base.njk
---

<article>
  {% if image %}
    <img src="{{ image }}" alt="{{ title }}">
  {% endif %}

  <h1>{{ title }}</h1>
  
  {% if date %}
    <time class="post-date" datetime="{{ date }}">{{ date }}</time>
  {% endif %}

  {{ content | safe }}
</article>
